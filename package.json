{
    "name": "speedtest-website",
    "version": "1.0.0",
    "description": "A website that runs Cloudflare speed tests.",
    "main": "server.js",
    "scripts": {
      "build": "npm run build:server && npm run build:client",
      "start": "node server.js"
    },
    "dependencies": {
      "@cloudflare/speedtest": "^1.0.0",
      "express": "^4.18.2",
      "cors": "^2.8.3"
    }
  }